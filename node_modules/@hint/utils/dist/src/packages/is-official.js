"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const find_package_root_1 = require("./find-package-root");
const fs_1 = require("../fs");
exports.isOfficial = async () => {
    try {
        const pkg = JSON.parse(await fs_1.readFile(path_1.join(find_package_root_1.findPackageRoot(process.cwd()), 'package.json')));
        return pkg.name === '@hint/monorepo';
    }
    catch (e) {
        return false;
    }
};
