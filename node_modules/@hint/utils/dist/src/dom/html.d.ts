import { ProblemLocation } from '../types/problem-location';
import { INamedNodeMap } from '../types/html';
import { DocumentData, ElementData } from '../types/snapshot';
export declare class HTMLElement {
    ownerDocument: HTMLDocument;
    private _element;
    constructor(element: ElementData | HTMLElement, ownerDocument: HTMLDocument);
    readonly attributes: INamedNodeMap;
    readonly children: HTMLElement[];
    readonly parentElement: HTMLElement | null;
    readonly nodeName: string;
    getAttribute(attribute: string): string | null;
    isAttributeAnExpression(attribute: string): boolean;
    private _getOriginalLocation;
    getLocation(): ProblemLocation;
    getContentLocation(offset: ProblemLocation): ProblemLocation | null;
    isSame(element: HTMLElement): boolean;
    readonly innerHTML: string;
    readonly outerHTML: string;
    resolveUrl(url: string): string;
}
export declare class HTMLDocument {
    private _document;
    private _documentElement;
    private _pageHTML;
    private _base;
    originalDocument?: HTMLDocument;
    constructor(document: DocumentData, finalHref: string, originalDocument?: HTMLDocument);
    private findDocumentElement;
    private getBaseUrl;
    readonly documentElement: HTMLElement;
    readonly base: string;
    readonly isFragment: boolean;
    pageHTML(): string;
    querySelectorAll(selector: string): HTMLElement[];
    resolveUrl(url: string): string;
}
//# sourceMappingURL=html.d.ts.map