"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const to_lowercase_array_1 = require("../misc/to-lowercase-array");
exports.includedHeaders = (headers, headerList = []) => {
    const result = [];
    const list = to_lowercase_array_1.toLowerCaseArray(headerList);
    for (const key of Object.keys(headers)) {
        const lowercaseKey = key.toLowerCase();
        if (list.includes(lowercaseKey)) {
            result.push(lowercaseKey);
        }
    }
    const shortedResult = result.sort();
    return shortedResult;
};
